"use client";

import { PropsUser } from "@/types/types";
import React from "react";

const ProfileClient = ({ user }: { user: PropsUser }) => {
  return <div>ProfileClient</div>;
};

export default ProfileClient;
